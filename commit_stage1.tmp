重构（阶段 1）：包入口不再聚合实现，测试显式导入 + 局部 patch

包入口减重
- 新增 src/macroflow/ui/app/main.py：MacroFlowApp 的组合定义
- src/macroflow/ui/app/__init__.py 从 292 行降到 16 行：只转出 MacroFlowApp
- startup.main() 改为 from .main import MacroFlowApp（去掉循环导入的按需导入）
- src/macroflow/ui/app/*.py 的对话框导入全部指向 dialogs.<功能> 子模块

测试减耦
- 12 个测试文件去掉 `from tests.common import *`，改为从各名字真正的定义模块导入
- tests/common.py 只保留导入路径配置与共用替身
- tests/helpers/patches.py：显式的 名字→实现模块 目标表；package_patch 取代
  「扫描包及所有子模块」的 _PackagePatch
- tests/helpers/patch_targets.json：由实现模块真实的 import / 使用点生成
- 781 处 patch_app/patch_dialogs/patch_player 改为 package_patch('app'|'dialogs'|'player', name)
- 97 处 patch("macroflow.ui.app.X") 目标改到真正定义 X 的实现模块
- 直接 patch 的模块级目标（tk/ttk/ModalDialog.__init__ 等）改为按定义模块 patch

验证
- python -m unittest discover -s tests -t .  → Ran 1212 tests, OK
- 编辑路径基准保持：单行编辑 p50/p95 = 0.01/0.02ms（1000–25000 条）
- 未启动 GUI；未做界面/视觉验收
